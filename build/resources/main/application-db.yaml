spring:
  data:
    mongodb:
      auto-index-creation: true
      authentication-database: admin
      username: docker
      password: password
      database: assigment
      port: 27017
      host: localhost

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              ReactiveMongoTemplate: DEBUG